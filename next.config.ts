// // next.config.js

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true, // Enforces best practices

// };

const nextConfig = {
  experimental: {
    appDir: true,
  },
};

module.exports = nextConfig;
